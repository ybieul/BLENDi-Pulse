# ─── BLENDi Pulse API — variáveis de ambiente ─────────────────────────────────
# Copie este arquivo para .env e preencha com os valores reais.
# NUNCA commite o arquivo .env.

# ── Servidor ──────────────────────────────────────────────────────────────────
PORT=3000
NODE_ENV=development          # development | staging | production
API_VERSION=0.1.0

# Loga OTP/código de verificação em texto claro no console (útil em dev local
# para testar o fluxo de recuperação de senha sem um provedor de e-mail real).
# NUNCA defina como true em produção — o OTP logado permite redefinir a senha
# de qualquer conta sem conhecer a senha atual.
ENABLE_DEV_EMAIL_LOG=true

# ── MongoDB Atlas ─────────────────────────────────────────────────────────────
# Formato: mongodb+srv://<user>:<password>@<cluster>.mongodb.net/<dbname>
MONGODB_URI=mongodb+srv://CHANGE_ME:CHANGE_ME@cluster0.xxxxx.mongodb.net/blendi-pulse

# ── JWT ───────────────────────────────────────────────────────────────────────
# Use `openssl rand -base64 64` para gerar segredos fortes
JWT_ACCESS_SECRET=CHANGE_ME_USE_OPENSSL_RAND
JWT_REFRESH_SECRET=CHANGE_ME_USE_OPENSSL_RAND
# Usado exclusivamente para tokens temporários de redefinição de senha (10 min)
JWT_RESET_SECRET=CHANGE_ME_USE_OPENSSL_RAND

# Access token expira em 15 minutos; refresh token em 30 dias
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=30d

# ── CORS ──────────────────────────────────────────────────────────────────────
# Origens permitidas separadas por vírgula
# Em produção: domínio do app (expo://..., https://...)
ALLOWED_ORIGINS=http://localhost:8081,http://localhost:19006
EXPO_ACCESS_TOKEN=CHANGE_ME

# ── Google OAuth 2.0 ──────────────────────────────────────────────────────────
# Passo a passo para obter as credenciais:
# 1. Acesse console.cloud.google.com
# 2. Crie (ou selecione) o projeto "blendi-pulse"
# 3. Menu lateral → APIs e Serviços → Biblioteca → ative "Google People API"
# 4. APIs e Serviços → Credenciais → Criar credenciais → ID do cliente OAuth 2.0
# 5. Tipo de aplicativo: "Aplicativo da Web"
# 6. Origens JavaScript autorizadas: http://localhost:3000
# 7. URIs de redirecionamento autorizados: http://localhost:3000/auth/google/callback
# 8. Copie o Client ID e o Client Secret gerados abaixo

GOOGLE_CLIENT_ID=CHANGE_ME.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=CHANGE_ME
# Dev: http://localhost:3000/auth/google/callback
# Prod: https://api.blendipulse.com/auth/google/callback
GOOGLE_REDIRECT_URI=http://localhost:3000/auth/google/callback

# ── AI Provider ──────────────────────────────────────────────────────────────
# Provider usado pelo Pulse AI Chat no backend.
# Valores permitidos: openai | anthropic | google
# Para Gemini neste código, use AI_PROVIDER=google.
AI_PROVIDER=google
# Exemplos: gpt-4o-mini | claude-sonnet-4-6 | gemini-3.5-flash-lite
# gemini-2.5-flash e gemini-2.5-pro foram descontinuados p/ chaves novas
# (404 "no longer available to new users"). gemini-3.5-flash (sem "-lite")
# tem "thinking" interno ligado por padrão e estoura os timeouts de 30s/45s
# em requests reais — gemini-3.5-flash-lite não usa thinking por padrão,
# responde em 1-3s e é o validado nesta app. Tier "pro" tem quota 0 no
# free tier, nem tente.
AI_MODEL=gemini-3.5-flash-lite
# Chave do provider escolhido acima.
AI_API_KEY=CHANGE_ME

# ── Vision AI Provider ──────────────────────────────────────────────────────
# Provider usado nas análises de imagem do Pantry Scanner.
# Valores permitidos: openai | anthropic | google
# Pode ser diferente do provider de chat acima.
VISION_PROVIDER=google
# Identificador exato do modelo de visão.
# gemini-3.5-flash-lite é multimodal (texto + imagem), sem "thinking" por
# padrão (rápido o bastante p/ os 45s de timeout) e funciona no tier
# gratuito; modelos "pro" (ex: gemini-3.1-pro-preview) retornam 429
# quota=0 no free tier.
VISION_MODEL=gemini-3.5-flash-lite
# Pode reutilizar a mesma chave de AI_API_KEY se o provider for o mesmo.
VISION_API_KEY=CHANGE_ME

# ── RevenueCat / Pagamentos ─────────────────────────────────────────────────x
# O backend continua subindo mesmo se estes valores estiverem vazios.
# Enquanto nao forem preenchidos, funcionalidades de assinatura ficam indisponiveis.
REVENUECAT_API_KEY=
# Valor do campo "Authorization header value" do webhook no painel do RevenueCat (HMAC
# desligado). Deve ser IDENTICO ao do painel, incluindo o prefixo "Bearer " se houver.
# So existe depois de configurar o webhook no painel apontando para a URL publica do backend.
REVENUECAT_WEBHOOK_SECRET=
# OBRIGATORIA (a API nao sobe sem ela). Trecho aleatorio da URL do webhook, que fica
# /webhooks/revenuecat/<valor>: a rota deixa de ser previsivel. Minimo 32 caracteres,
# apenas [A-Za-z0-9_-] (hex ou base64url). Gere com: openssl rand -hex 32
# A mesma URL completa deve ser configurada no painel do RevenueCat. Nao commite o valor real.
REVENUECAT_WEBHOOK_PATH_SECRET=
# app_ids do RevenueCat aceitos no webhook, separados por virgula (um por plataforma:
# iOS e Android sao apps distintos). Ex.: REVENUECAT_APP_IDS=appIOS...,appANDROID...
REVENUECAT_APP_IDS=
# Legado (um unico app_id) — ainda aceito; prefira REVENUECAT_APP_IDS.
REVENUECAT_APP_ID=
