#!/bin/sh

set -eu

SCRIPT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
TARGET="$SCRIPT_DIR/../../../node_modules/react-native/sdks/hermesc/osx-bin/hermesc"

if [ ! -x "$TARGET" ]; then
  echo "[hermes-compiler shim] Missing target binary: $TARGET" >&2
  exit 1
fi

exec "$TARGET" "$@"