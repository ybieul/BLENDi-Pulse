# BLENDi Pulse API — Endpoint Reference

BLENDi Pulse API follows a REST-style design and returns JSON for application endpoints, with one redirect-based OAuth callback flow. Authentication-protected routes use JWT Bearer tokens sent in the `Authorization` header as `Bearer <accessToken>`. Successful application responses use `success: true`, while error responses use `success: false` plus a stable machine-readable `code` and an English `message`. Development base URL is `http://localhost:3000`. The production base URL will be defined after the Railway deployment configuration is finalized.

> Exceptions: `GET /ping` returns a plain JSON health payload without the `success` envelope, and `GET /auth/google/callback` returns an HTTP redirect to the mobile deep link instead of JSON.

---

## Base Conventions

### Authorization Header

```http
Authorization: Bearer <accessToken>
```

### Content Type

```http
Content-Type: application/json
Accept: application/json
```

### Message Format

The API returns a stable top-level error `code` such as `auth/invalid-credentials` together with an English `message`. When validation fails, the nested `errors` array still uses field-level messages generated by the shared Zod schemas, which may be i18n-style keys such as `errors.validation.required`.

---

## Authentication

### POST /auth/register

Creates a new user account and returns the initial session tokens.

**JWT required:** No

**Request body**

| Field | Type | Required | Notes |
|---|---|---|---|
| `email` | string | Yes | Valid email address, normalized to lowercase |
| `password` | string | Yes | Minimum 8 characters, maximum 72, must include uppercase, lowercase, and a digit |
| `name` | string | Yes | 2 to 60 characters |
| `blendiModel` | string | Yes | One of `Lite`, `ProPlus`, `Steel` |
| `goal` | string | Yes | One of `Muscle`, `Wellness`, `Energy`, `Recovery` |
| `preferredLanguage` | string | No | One of `en`, `pt-BR`; defaults to `en` when omitted |
| `dailyProteinTarget` | number | Yes | Integer between 10 and 400 |
| `dailyCalorieTarget` | number | Yes | Integer between 500 and 10000 |
| `timezone` | string | Yes | IANA timezone string such as `America/Sao_Paulo` |

**Success response**

**Status:** `201 Created`

```json
{
  "success": true,
  "data": {
    "user": {
      "id": "string",
      "email": "string",
      "name": "string",
      "blendiModel": "Lite | ProPlus | Steel",
      "goal": "Muscle | Wellness | Energy | Recovery",
      "locale": "en | pt-BR",
      "timezone": "string",
      "dailyProteinTarget": 150,
      "dailyCalorieTarget": 2000,
      "createdAt": "2026-04-29T12:34:56.000Z"
    },
    "accessToken": "string",
    "refreshToken": "string"
  }
}
```

**Status codes**

| Status | Meaning |
|---|---|
| `201` | User created successfully |
| `400` | Request body failed Zod validation |
| `409` | Email already exists |
| `500` | Unexpected server error |

### POST /auth/login

Authenticates an existing user and returns a fresh access token and refresh token.

**JWT required:** No

**Request body**

| Field | Type | Required | Notes |
|---|---|---|---|
| `email` | string | Yes | Valid email address, normalized to lowercase |
| `password` | string | Yes | Raw password entered by the user |

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "user": {
      "id": "string",
      "email": "string",
      "name": "string",
      "blendiModel": "Lite | ProPlus | Steel",
      "goal": "Muscle | Wellness | Energy | Recovery",
      "locale": "en | pt-BR",
      "timezone": "string",
      "dailyProteinTarget": 150,
      "dailyCalorieTarget": 2000,
      "createdAt": "2026-04-29T12:34:56.000Z"
    },
    "accessToken": "string",
    "refreshToken": "string"
  }
}
```

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Login successful |
| `400` | Request body failed Zod validation |
| `401` | Invalid credentials; response is intentionally generic for security and does not reveal whether the email exists |
| `500` | Unexpected server error |

### POST /auth/refresh

Rotates the refresh token and returns a brand-new token pair.

**JWT required:** No

**Request body**

| Field | Type | Required | Notes |
|---|---|---|---|
| `refreshToken` | string | Yes | Previously issued refresh token |

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "accessToken": "string",
    "refreshToken": "string"
  }
}
```

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Refresh token accepted and rotated successfully |
| `400` | Request body failed Zod validation |
| `401` | Refresh token invalid, expired, or user no longer active |
| `500` | Unexpected server error |

### GET /auth/google/url

Returns the Google OAuth authorization URL that the mobile app must open using `expo-web-browser`.

**JWT required:** No

**Request parameters**

None.

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "url": "https://accounts.google.com/o/oauth2/v2/auth?..."
  }
}
```

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Authorization URL generated successfully |
| `500` | Unexpected server error |

### GET /auth/google/callback

Google OAuth callback endpoint. This endpoint is called by Google after the user authorizes access and is not meant to be called directly by the mobile client.

The backend validates the callback, exchanges the authorization code, logs the user in or creates a new account when needed, and then redirects the browser to the mobile deep link.

**JWT required:** No

**Query parameters**

| Field | Type | Required | Notes |
|---|---|---|---|
| `code` | string | Usually yes | Authorization code returned by Google |
| `state` | string | Yes | CSRF protection token originally issued by `GET /auth/google/url`; implemented as a short-lived JWT signed by the backend |
| `error` | string | No | Present when the user cancels or Google returns an auth error |

**Success behavior**

**Status:** `302 Found`

Redirects to:

```text
blendipulse://auth/callback?accessToken=<jwt>&refreshToken=<jwt>&isNewUser=<true|false>&user=<base64-json>
```

The `user` parameter is a Base64-encoded JSON payload containing:

| Field | Type |
|---|---|
| `id` | string |
| `email` | string |
| `name` | string |
| `profilePhoto` | string or undefined |
| `blendiModel` | string |
| `goal` | string |
| `locale` | string |
| `timezone` | string |
| `dailyProteinTarget` | number |
| `dailyCalorieTarget` | number |
| `createdAt` | ISO datetime string |

**Error behavior**

Handled OAuth failures also return a redirect instead of JSON. The backend redirects with an i18n error key:

```text
blendipulse://auth/callback?error=errors.auth.google_auth_failed
```

Possible error keys include `errors.auth.google_cancelled`, `errors.auth.invalid_state`, `errors.validation.required`, and `errors.auth.google_auth_failed`.

**Status codes**

| Status | Meaning |
|---|---|
| `302` | Redirect to the mobile deep link after successful or handled failed OAuth flow |
| `500` | Unexpected server error |

### POST /auth/forgot-password

Starts the password reset flow by generating an OTP and attempting to send it by email.

**JWT required:** No

**Request body**

| Field | Type | Required | Notes |
|---|---|---|---|
| `email` | string | Yes | Valid email address |

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "message": "errors.auth.forgot_password_sent"
  }
}
```

This endpoint always returns the same `200` response regardless of whether the email exists. This is intentional and prevents attackers from discovering registered accounts through response differences.

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Request accepted; response is identical for existing and non-existing emails |
| `400` | Request body failed Zod validation |
| `500` | Unexpected server error before the immediate response is sent |

### POST /auth/verify-otp

Validates the six-digit OTP sent by email and returns a temporary password reset token.

**JWT required:** No

**Request body**

| Field | Type | Required | Notes |
|---|---|---|---|
| `email` | string | Yes | Valid email address |
| `otp` | string | Yes | Exactly 6 numeric digits |

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "resetToken": "string"
  }
}
```

The `resetToken` is a JWT signed with a dedicated reset secret and valid for 10 minutes.

**Status codes**

| Status | Meaning |
|---|---|
| `200` | OTP accepted; temporary reset token issued |
| `400` | Request body failed validation, or OTP is invalid, expired, already used, or blocked after too many attempts |
| `500` | Unexpected server error |

### PATCH /auth/reset-password

Resets the account password using the temporary reset token issued by `POST /auth/verify-otp`.

**JWT required:** No

**Request body**

| Field | Type | Required | Notes |
|---|---|---|---|
| `resetToken` | string | Yes | JWT issued by `POST /auth/verify-otp` |
| `newPassword` | string | Yes | Minimum 8 characters, maximum 72, must include uppercase, lowercase, and a digit |

For the token to be accepted, its payload must include `purpose: password_reset`.

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "message": "errors.auth.password_reset_success"
  }
}
```

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Password reset completed successfully |
| `400` | Request body failed validation, reset token expired, token payload invalid, token purpose invalid, or target user not found |
| `401` | Reset token already used because the password was already changed after the token was issued |
| `500` | Unexpected server error |

---

## Users

### GET /users/me

Returns the full profile of the authenticated user.

**JWT required:** Yes

**Request parameters**

None.

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "user": {
      "id": "string",
      "name": "string",
      "email": "string",
      "profilePhoto": "https://cdn.example.com/avatar.png",
      "blendiModel": "Lite | ProPlus | Steel",
      "goal": "Muscle | Wellness | Energy | Recovery",
      "preferredLanguage": "en | pt-BR",
      "timezone": "America/Sao_Paulo",
      "unitSystem": "metric | imperial",
      "dailyProteinTarget": 150,
      "dailyCarbTarget": 220,
      "dailyCalorieTarget": 2400,
      "dailyHydrationTarget": 2500,
      "isPro": false,
      "currentStreak": 5,
      "longestStreak": 12,
      "blendCount": 28,
      "createdAt": "2026-04-29T12:34:56.000Z",
      "lastCleanedAt": "2026-05-20T08:15:00.000Z"
    }
  }
}
```

The current implementation also returns compatibility aliases `streakDays` and `totalBlends` with the same numeric values as `currentStreak` and `blendCount`.

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Profile returned successfully |
| `401` | Missing, invalid, or expired Bearer token |
| `404` | Authenticated user record not found |
| `500` | Unexpected server error |

### PATCH /users/me

Partially updates the authenticated user's profile and goals.

**JWT required:** Yes

**Request body**

All fields are optional. The endpoint only updates fields that are present in the request body.

| Field | Type | Required | Notes |
|---|---|---|---|
| `blendiModel` | string | No | One of `Lite`, `ProPlus`, `Steel` |
| `goal` | string | No | One of `Muscle`, `Wellness`, `Energy`, `Recovery` |
| `dailyProteinTarget` | number | No | Integer between 10 and 400 |
| `dailyCarbTarget` | number | No | Integer between 50 and 800 |
| `dailyCalorieTarget` | number | No | Integer between 500 and 10000 |
| `dailyHydrationTarget` | number | No | Integer between 500 and 8000 |
| `weight` | number | No | Number between 20 and 300 |
| `height` | number | No | Number between 100 and 250 |
| `preferredLanguage` | string | No | One of `en`, `pt-BR`; stored internally as `locale` |
| `unitSystem` | string | No | One of `metric`, `imperial` |

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "user": {
      "id": "string",
      "email": "string",
      "name": "string",
      "profilePhoto": "https://cdn.example.com/avatar.png",
      "blendiModel": "Lite | ProPlus | Steel",
      "goal": "Muscle | Wellness | Energy | Recovery",
      "locale": "en | pt-BR",
      "unitSystem": "metric | imperial",
      "timezone": "America/Sao_Paulo",
      "dailyProteinTarget": 160,
      "dailyCalorieTarget": 2500,
      "dailyCarbTarget": 240,
      "dailyHydrationTarget": 3000,
      "isPro": false,
      "longestStreak": 12,
      "weight": 78,
      "height": 180,
      "createdAt": "2026-04-29T12:34:56.000Z",
      "updatedAt": "2026-05-26T14:20:00.000Z"
    }
  }
}
```

The request field `preferredLanguage` is normalized to the persisted `locale` field. The current PATCH response echoes `locale` rather than `preferredLanguage`.

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Profile updated successfully |
| `400` | Request body failed Zod validation |
| `401` | Missing, invalid, or expired Bearer token |
| `404` | Authenticated user record not found |
| `500` | Unexpected server error |

### PATCH /users/me/cleaned

Updates `lastCleanedAt` for the authenticated user to the current server timestamp.

**JWT required:** Yes

**Request body**

None.

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "user": {
      "id": "string",
      "lastCleanedAt": "2026-05-26T14:20:00.000Z",
      "updatedAt": "2026-05-26T14:20:00.000Z"
    }
  }
}
```

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Cleaning timestamp updated successfully |
| `401` | Missing, invalid, or expired Bearer token |
| `404` | Authenticated user record not found |
| `500` | Unexpected server error |

### POST /users/calculate-macros

Calculates IMC, TDEE, and daily macro targets for onboarding and profile setup.

**JWT required:** Yes

**Request body**

| Field | Type | Required | Notes |
|---|---|---|---|
| `weight` | number | Yes | Positive number; interpreted according to `unitSystem` |
| `height` | number | Yes | Positive number; interpreted according to `unitSystem` |
| `activityLevel` | string | Yes | One of `sedentary`, `lightlyActive`, `moderatelyActive`, `veryActive` |
| `goal` | string | Yes | One of `Muscle`, `Wellness`, `Energy`, `Recovery` |
| `unitSystem` | string | Yes | One of `metric`, `imperial` |

The current implementation converts imperial values to metric when needed, computes IMC, then derives TDEE and the daily targets using the project's current calorie and protein multipliers with a fixed assumed age of 30.

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "imc": 24.1,
    "imcUnit": "kg/m²",
    "imcClassification": "normal",
    "dailyCalorieTarget": 2450,
    "dailyProteinTarget": 156,
    "dailyCarbTarget": 284,
    "tdee": 2150
  }
}
```

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Macro targets calculated successfully |
| `400` | Request body failed Zod validation |
| `401` | Missing, invalid, or expired Bearer token |
| `500` | Unexpected server error |

### PATCH /auth/timezone

Updates the authenticated user's stored IANA timezone. This endpoint is still used by the mobile app to synchronize timezone changes detected on the device.

**JWT required:** Yes

**Request body**

| Field | Type | Required | Notes |
|---|---|---|---|
| `timezone` | string | Yes | IANA timezone string such as `America/Sao_Paulo` |

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "id": "string",
    "timezone": "America/Sao_Paulo"
  }
}
```

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Timezone updated successfully |
| `400` | Request body failed Zod validation |
| `401` | Missing, invalid, or expired Bearer token |
| `404` | Authenticated user record not found |
| `500` | Unexpected server error |

---

## Push Notifications Configuration

### PATCH /users/push-token

Stores or updates the authenticated user's push token used by Expo notifications.

**JWT required:** Yes

**Request body**

| Field | Type | Required | Notes |
|---|---|---|---|
| `pushToken` | string | Yes | Expo Push token in the `ExponentPushToken[...]` format |

If the incoming token is identical to the token already stored on the user document, the endpoint returns the current value without performing a database update.

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "user": {
      "id": "string",
      "pushToken": "ExponentPushToken[xxxxxxxxxxxxxxxxxxxxxx]",
      "updatedAt": "2026-06-26T14:20:00.000Z"
    }
  }
}
```

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Push token stored or confirmed successfully |
| `400` | Request body failed validation |
| `401` | Missing, invalid, or expired Bearer token |
| `404` | Authenticated user record not found |
| `500` | Unexpected server error |

### PATCH /users/notification-preferences

Partially updates the authenticated user's notification preference flags.

**JWT required:** Yes

**Request body**

All fields are optional. Any combination can be sent in the same request.

| Field | Type | Required | Notes |
|---|---|---|---|
| `dailyPulse` | boolean | No | Enables the Daily Pulse notification |
| `streakReminder` | boolean | No | Enables streak reminder notifications |
| `supplementReminder` | boolean | No | Enables supplement reminder notifications |
| `hydrationReminder` | boolean | No | Enables hydration reminder notifications |
| `levelUp` | boolean | No | Enables level-up notifications |

The controller uses `$set` with dot notation such as `notificationPreferences.dailyPulse`, so omitted fields are preserved instead of overwritten.

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "user": {
      "id": "string",
      "notificationPreferences": {
        "dailyPulse": true,
        "streakReminder": true,
        "supplementReminder": false,
        "hydrationReminder": true,
        "levelUp": true
      },
      "updatedAt": "2026-06-26T14:20:00.000Z"
    }
  }
}
```

If the request body contains no recognized preference fields, the endpoint returns the current stored preferences unchanged.

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Notification preferences merged successfully |
| `400` | Request body failed validation |
| `401` | Missing, invalid, or expired Bearer token |
| `404` | Authenticated user record not found |
| `500` | Unexpected server error |

### PATCH /users/daily-pulse-time

Updates the preferred local delivery time for Daily Pulse notifications.

**JWT required:** Yes

**Request body**

| Field | Type | Required | Notes |
|---|---|---|---|
| `hour` | number | Yes | Integer between `0` and `23` |
| `minute` | number | Yes | Integer between `0` and `59` |

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "user": {
      "id": "string",
      "dailyPulseTime": {
        "hour": 7,
        "minute": 30
      },
      "updatedAt": "2026-06-26T14:20:00.000Z"
    }
  }
}
```

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Daily Pulse time updated successfully |
| `400` | Request body failed validation |
| `401` | Missing, invalid, or expired Bearer token |
| `404` | Authenticated user record not found |
| `500` | Unexpected server error |

---

## Blend Logs

All Blend Log history ranges interpret `from` and `to` in the authenticated user's timezone rather than raw UTC midnight.

### POST /blend-logs

Creates a new blend log and updates the user's derived streak and blend counters.

**JWT required:** Yes

**Request body**

| Field | Type | Required | Notes |
|---|---|---|---|
| `recipeName` | string | No | 1 to 100 characters when present |
| `protein` | number | Yes | Minimum 0; defaults to `0` when omitted |
| `carbs` | number | Yes | Minimum 0; defaults to `0` when omitted |
| `fat` | number | Yes | Minimum 0; defaults to `0` when omitted |
| `calories` | number | Yes | Integer, minimum 0; defaults to `0` when omitted |
| `blendiModel` | string | Yes | One of `Lite`, `ProPlus`, `Steel` |
| `durationSeconds` | number | Yes | Integer between 5 and 300 |
| `fromFavoriteId` | string | No | Present only when the blend was started from a `FavoriteCard`; used to trigger the `makeBlendFromFavorite` mission |
| `rating` | number | No | Integer between 1 and 5 |

**Success response**

**Status:** `201 Created`

```json
{
  "success": true,
  "data": {
    "log": {
      "id": "string",
      "recipeName": "Berry Blast",
      "protein": 30,
      "carbs": 22,
      "fat": 6,
      "calories": 280,
      "blendiModel": "ProPlus",
      "durationSeconds": 45,
      "rating": 5,
      "createdAt": "2026-05-26T14:20:00.000Z"
    },
    "currentStreak": 5,
    "longestStreak": 12,
    "blendCount": 28,
    "xpAwarded": 38,
    "leveledUp": false,
    "newLevel": null,
    "totalBlends": 28
  }
}
```

`xpAwarded` is the sum of direct blend XP, any protein or calorie goal XP unlocked by this blend, and any mission XP completed by the same action. `leveledUp` and `newLevel` are derived by comparing the level before and after all XP operations executed in parallel.

**Status codes**

| Status | Meaning |
|---|---|
| `201` | Blend log created successfully |
| `400` | Request body failed Zod validation |
| `401` | Missing, invalid, or expired Bearer token |
| `404` | Authenticated user record not found |
| `500` | Unexpected server error |

### GET /blend-logs/today

Returns the current day's blend logs in the authenticated user's timezone together with macro totals.

**JWT required:** Yes

**Request parameters**

None.

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "totalProtein": 60,
    "totalCarbs": 44,
    "totalCalories": 560,
    "blendCount": 2,
    "logs": [
      {
        "id": "string",
        "recipeName": "Berry Blast",
        "protein": 30,
        "carbs": 22,
        "fat": 6,
        "calories": 280,
        "blendiModel": "ProPlus",
        "durationSeconds": 45,
        "rating": 5,
        "createdAt": "2026-05-26T14:20:00.000Z"
      }
    ]
  }
}
```

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Today's blend logs returned successfully |
| `401` | Missing, invalid, or expired Bearer token |
| `404` | Authenticated user record not found |
| `500` | Unexpected server error |

### GET /blend-logs/history

Returns paginated blend logs for a date range plus interval aggregates and a per-day breakdown.

**JWT required:** Yes

**Query parameters**

| Field | Type | Required | Notes |
|---|---|---|---|
| `from` | string | Yes | Parseable date string representing the first local day in the range |
| `to` | string | Yes | Parseable date string representing the last local day in the range |
| `page` | number | No | Positive integer; defaults to `1` |
| `limit` | number | No | Integer between `1` and `100`; defaults to `30` |

`from` and `to` are interpreted in the authenticated user's timezone. The shared validator requires `from < to` and caps the maximum range at 365 days.

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "logs": [
      {
        "id": "string",
        "recipeName": "Berry Blast",
        "protein": 30,
        "carbs": 22,
        "fat": 6,
        "calories": 280,
        "blendiModel": "ProPlus",
        "durationSeconds": 45,
        "rating": 5,
        "createdAt": "2026-05-26T14:20:00.000Z"
      }
    ],
    "summary": {
      "totalProtein": 300,
      "totalCarbs": 220,
      "totalFat": 60,
      "totalCalories": 2800,
      "blendCount": 10,
      "averageDailyProtein": 60,
      "averageDailyCalories": 560
    },
    "dailyBreakdown": [
      {
        "date": "2026-05-26",
        "count": 2,
        "protein": 60,
        "carbs": 44,
        "calories": 560
      }
    ],
    "total": 10,
    "page": 1,
    "totalPages": 1
  }
}
```

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Blend history returned successfully |
| `400` | Query parameters failed validation |
| `401` | Missing, invalid, or expired Bearer token |
| `404` | Authenticated user record not found |
| `500` | Unexpected server error |

---

## Hydration

All Hydration history ranges interpret `from` and `to` in the authenticated user's timezone rather than raw UTC midnight.

### POST /hydration-logs

Registers a hydration entry and returns the updated total for the current local day.

**JWT required:** Yes

**Request body**

| Field | Type | Required | Notes |
|---|---|---|---|
| `amountMl` | number | No | Positive integer; defaults to `250` when omitted |

**Success response**

**Status:** `201 Created`

```json
{
  "success": true,
  "data": {
    "log": {
      "id": "string",
      "amountMl": 250,
      "createdAt": "2026-05-26T14:20:00.000Z"
    },
    "totalMl": 750,
    "goalMl": 2500,
    "xpAwarded": 5
  }
}
```

The current response field for the user's daily hydration target is `goalMl`. `xpAwarded` is the hydration-goal XP for the current local day and is awarded only once per day when the updated total reaches or exceeds the user's target; otherwise it is `0`.

**Status codes**

| Status | Meaning |
|---|---|
| `201` | Hydration log created successfully |
| `400` | `amountMl` failed validation |
| `401` | Missing, invalid, or expired Bearer token |
| `404` | Authenticated user record not found |
| `500` | Unexpected server error |

### GET /hydration-logs/today

Returns the hydration total for the current local day and the configured daily target.

**JWT required:** Yes

**Request parameters**

None.

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "totalMl": 750,
    "goalMl": 2500,
    "logs": [
      {
        "id": "string",
        "amountMl": 250,
        "createdAt": "2026-05-26T14:20:00.000Z"
      }
    ]
  }
}
```

The configured daily target is returned as `goalMl` in the current payload.

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Today's hydration summary returned successfully |
| `401` | Missing, invalid, or expired Bearer token |
| `404` | Authenticated user record not found |
| `500` | Unexpected server error |

### GET /hydration-logs/history

Returns paginated hydration logs for a date range plus total and average daily hydration.

**JWT required:** Yes

**Query parameters**

| Field | Type | Required | Notes |
|---|---|---|---|
| `from` | string | Yes | Parseable date string representing the first local day in the range |
| `to` | string | Yes | Parseable date string representing the last local day in the range |
| `page` | number | No | Positive integer; defaults to `1` |
| `limit` | number | No | Integer between `1` and `100`; defaults to `30` |

`from` and `to` are interpreted in the authenticated user's timezone. The shared validator requires `from < to` and caps the maximum range at 365 days.

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "logs": [
      {
        "id": "string",
        "amountMl": 250,
        "createdAt": "2026-05-26T14:20:00.000Z"
      }
    ],
    "summary": {
      "totalMl": 3000,
      "averageDailyMl": 600
    },
    "dailyBreakdown": [
      {
        "date": "2026-05-26",
        "totalMl": 750
      }
    ],
    "dailyHydrationTarget": 2500,
    "total": 12,
    "page": 1,
    "totalPages": 1
  }
}
```

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Hydration history returned successfully |
| `400` | Query parameters failed validation |
| `401` | Missing, invalid, or expired Bearer token |
| `404` | Authenticated user record not found |
| `500` | Unexpected server error |

---

## Supplement Stack

Supplement day-based operations use the authenticated user's timezone.

### GET /supplement-stack

Returns the authenticated user's full supplement stack, including active and inactive items, with today's progress metadata for each supplement.

**JWT required:** Yes

**Request parameters**

None.

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "stack": [
      {
        "supplementId": "string",
        "name": "Creatine",
        "dosage": "5g",
        "timing": "postWorkout",
        "isActive": true,
        "order": 0,
        "dailyTargetCount": 1,
        "consumedTodayCount": 1,
        "checkedToday": true,
        "checkedAt": "2026-05-26T14:20:00.000Z"
      }
    ]
  }
}
```

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Supplement stack returned successfully |
| `401` | Missing, invalid, or expired Bearer token |
| `404` | Authenticated user record not found |
| `500` | Unexpected server error |

### PUT /supplement-stack

Replaces the authenticated user's entire supplement stack.

**JWT required:** Yes

**Request body**

Root payload: array of supplement items, maximum 20 entries.

| Field | Type | Required | Notes |
|---|---|---|---|
| `name` | string | Yes | 1 to 60 characters |
| `dosage` | string | Yes | 1 to 30 characters |
| `dailyTargetCount` | number | No | Integer between 1 and 20; defaults to `1` |
| `timing` | string | Yes | One of `morning`, `preWorkout`, `postWorkout`, `evening`, `withMeal` |
| `isActive` | boolean | No | Defaults to `true` |

When an item already carries an existing `supplementId`, the backend preserves that identifier; new items receive generated UUIDs.

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "stack": [
      {
        "supplementId": "string",
        "name": "Creatine",
        "dosage": "5g",
        "timing": "postWorkout",
        "isActive": true,
        "order": 0,
        "dailyTargetCount": 1
      }
    ]
  }
}
```

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Supplement stack replaced successfully |
| `400` | Request body failed Zod validation |
| `401` | Missing, invalid, or expired Bearer token |
| `404` | Authenticated user record not found |
| `500` | Unexpected server error |

### POST /supplement-stack/:id/check

Creates or increments the current local-day `SupplementLog` for a supplement.

**JWT required:** Yes

**Path parameters**

| Field | Type | Required | Notes |
|---|---|---|---|
| `id` | string | Yes | Existing `supplementId` from the user's stack |

**Success response**

**Status:** `201 Created` on the first check of the day, or `200 OK` when incrementing an existing log.

```json
{
  "success": true,
  "data": {
    "log": {
      "id": "string",
      "supplementId": "string",
      "supplementName": "Creatine",
      "logDate": "2026-05-26",
      "consumedCount": 1,
      "createdAt": "2026-05-26T14:20:00.000Z"
    },
    "xpAwarded": 5
  }
}
```

`xpAwarded` is the supplement-goal XP for the day. It is awarded only once per local day, and only when this check causes all active supplements to meet their respective daily target counts; subsequent checks return `0`.

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Existing supplement log incremented successfully |
| `201` | First supplement log of the local day created successfully |
| `401` | Missing, invalid, or expired Bearer token |
| `404` | Authenticated user record or supplement not found |
| `500` | Unexpected server error |

### DELETE /supplement-stack/:id/check

Removes today's supplement check progress for a specific item. If the current local-day log has `consumedCount > 1`, the backend decrements the count; otherwise it deletes the log.

**JWT required:** Yes

**Path parameters**

| Field | Type | Required | Notes |
|---|---|---|---|
| `id` | string | Yes | Existing `supplementId` from the user's stack |

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "message": "Supplement progress decremented successfully."
  }
}
```

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Supplement progress removed successfully |
| `401` | Missing, invalid, or expired Bearer token |
| `404` | Authenticated user record or current-day supplement log not found |
| `500` | Unexpected server error |

### DELETE /supplement-stack/:id

Deletes a supplement from the authenticated user's stack and removes all of its historical logs.

**JWT required:** Yes

**Path parameters**

| Field | Type | Required | Notes |
|---|---|---|---|
| `id` | string | Yes | Existing `supplementId` from the user's stack |

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "stack": [
      {
        "supplementId": "string",
        "name": "Omega-3",
        "dosage": "2 softgels",
        "timing": "withMeal",
        "isActive": true,
        "order": 0,
        "dailyTargetCount": 1
      }
    ]
  }
}
```

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Supplement and historical logs removed successfully |
| `401` | Missing, invalid, or expired Bearer token |
| `404` | Authenticated user record or supplement not found |
| `500` | Unexpected server error |

### GET /supplement-logs/history

Returns day-by-day supplement adherence for the requested date range.

**JWT required:** Yes

**Query parameters**

| Field | Type | Required | Notes |
|---|---|---|---|
| `from` | string | Yes | Parseable date string representing the first local day in the range |
| `to` | string | Yes | Parseable date string representing the last local day in the range |

The endpoint reuses the shared history validator, so optional `page` and `limit` values are accepted for contract consistency, but the current response is day-based and not paginated.

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "history": [
      {
        "date": "2026-05-26",
        "checkedSupplements": [
          {
            "supplementId": "string",
            "name": "Creatine",
            "dosage": "5g",
            "timing": "postWorkout",
            "isActive": true,
            "order": 0,
            "dailyTargetCount": 1
          }
        ],
        "missedSupplements": [],
        "adherenceRate": 100
      }
    ],
    "summary": {
      "averageAdherence": 82.5
    }
  }
}
```

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Supplement history returned successfully |
| `400` | Query parameters failed validation |
| `401` | Missing, invalid, or expired Bearer token |
| `404` | Authenticated user record not found |
| `500` | Unexpected server error |

---

## Favorites

### GET /favorites

Returns the authenticated user's favorites in reverse chronological order.

**JWT required:** Yes

**Query parameters**

| Field | Type | Required | Notes |
|---|---|---|---|
| `limit` | number | No | Positive integer; when omitted, returns the full list |

Invalid `limit` values are ignored and do not trigger a validation error.

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "favorites": [
      {
        "id": "string",
        "recipeName": "Berry Blast",
        "ingredients": [
          {
            "name": "Blueberries",
            "amount": "100 g"
          }
        ],
        "protein": 30,
        "carbs": 22,
        "fat": 6,
        "calories": 280,
        "prepTimeSeconds": 60,
        "blendInstruction": "Blend for 45 seconds.",
        "tip": "Use frozen fruit for a thicker texture.",
        "hasSubstitutes": false,
        "blendiModel": "ProPlus",
        "goal": "Muscle",
        "createdAt": "2026-05-26T14:20:00.000Z"
      }
    ],
    "total": 12
  }
}
```

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Favorites returned successfully |
| `401` | Missing, invalid, or expired Bearer token |
| `500` | Unexpected server error |

### POST /favorites

Creates a new favorite recipe for the authenticated user.

**JWT required:** Yes

**Request body**

| Field | Type | Required | Notes |
|---|---|---|---|
| `recipeName` | string | Yes | 1 to 100 characters |
| `ingredients` | array | Yes | At least one ingredient; each item requires `name` and `amount` |
| `protein` | number | Yes | Minimum 0 |
| `carbs` | number | Yes | Minimum 0 |
| `fat` | number | Yes | Minimum 0 |
| `calories` | number | Yes | Integer, minimum 0 |
| `prepTimeSeconds` | number | Yes | Integer, minimum 1 |
| `blendInstruction` | string | Yes | Non-empty string |
| `tip` | string | No | Optional non-empty string |
| `hasSubstitutes` | boolean | No | Defaults to `false` |

The backend checks duplicates by `recipeName` within the authenticated user's favorites. If a matching favorite already exists, the endpoint returns the existing record and `alreadyExists: true` instead of creating a duplicate.

**Success response**

**Status:** `201 Created` for a new favorite, or `200 OK` when the favorite already exists.

```json
{
  "success": true,
  "data": {
    "favorite": {
      "id": "string",
      "recipeName": "Berry Blast",
      "ingredients": [
        {
          "name": "Blueberries",
          "amount": "100 g"
        }
      ],
      "protein": 30,
      "carbs": 22,
      "fat": 6,
      "calories": 280,
      "prepTimeSeconds": 60,
      "blendInstruction": "Blend for 45 seconds.",
      "tip": "Use frozen fruit for a thicker texture.",
      "hasSubstitutes": false,
      "blendiModel": "ProPlus",
      "goal": "Muscle",
      "createdAt": "2026-05-26T14:20:00.000Z"
    },
    "alreadyExists": false,
    "xpAwarded": 2
  }
}
```

`xpAwarded` is the multi-occurrence favorite-save XP granted only when a new favorite is created. When the endpoint returns `alreadyExists: true`, no XP is awarded and the field is omitted.

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Favorite already existed and was returned without duplication |
| `201` | Favorite created successfully |
| `400` | Request body failed Zod validation |
| `401` | Missing, invalid, or expired Bearer token |
| `404` | Authenticated user record not found |
| `500` | Unexpected server error |

### DELETE /favorites/:id

Deletes a favorite belonging to the authenticated user.

**JWT required:** Yes

**Path parameters**

| Field | Type | Required | Notes |
|---|---|---|---|
| `id` | string | Yes | MongoDB ObjectId of the favorite to delete |

The backend verifies ownership before deleting. If the favorite exists but belongs to a different user, the endpoint returns `403 Forbidden`.

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "message": "Favorite removed successfully."
  }
}
```

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Favorite removed successfully |
| `400` | Favorite id is not a valid MongoDB ObjectId |
| `401` | Missing, invalid, or expired Bearer token |
| `403` | Favorite exists but does not belong to the authenticated user |
| `404` | Favorite not found |
| `500` | Unexpected server error |

---

## Pulse AI

### POST /pulse-ai/chat

Generates a structured Pulse AI recipe for the authenticated user.

**JWT required:** Yes

**Request body**

| Field | Type | Required | Notes |
|---|---|---|---|
| `message` | string | Yes | 1 to 500 characters |
| `language` | string | No | One of `en`, `pt-BR`; when present, overrides the profile language for this request |

The `language` field has priority over the user's stored `preferredLanguage` / `locale`. This override affects prompt generation, cache key generation, and the response content.

**Processing pipeline**

1. Validates the request body against `pulseAiChatSchema`.
2. Loads the authenticated user's Pulse AI profile and resets daily usage if the user's local day has changed.
3. Reserves usage atomically for non-Pro users with `findOneAndUpdate` and `dailyAiUsage: { $lt: 3 }`.
4. Normalizes the message, generates a MongoDB cache key using user context plus `AI_PROVIDER` and `AI_MODEL`, and checks the `AiCache` collection.
5. On a cache hit, returns the cached recipe immediately with `fromCache: true` and the remaining daily quota.
6. On a cache miss, fetches the last 5 blend recipe names for prompt context.
7. Builds the final prompt through `promptBuilder.service`.
8. Calls the configured AI provider abstraction, which is selected at runtime through `AI_PROVIDER`, `AI_MODEL`, and `AI_API_KEY`.
9. Parses the returned JSON and validates it against `pulseAiRecipeSchema`; if the first response is invalid JSON, the backend retries once with a strict JSON-only instruction.
10. Persists the validated recipe in MongoDB cache and returns it with `fromCache: false` and `usageRemaining`.

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "recipe": {
      "title": "Berry Blast",
      "ingredients": [
        {
          "name": "Blueberries",
          "amount": "100 g"
        }
      ],
      "macros": {
        "protein": 30,
        "carbs": 22,
        "fat": 6,
        "calories": 280
      },
      "prepTimeSeconds": 60,
      "blendInstruction": "Blend for 45 seconds.",
      "tip": "Use frozen fruit for a thicker texture.",
      "hasSubstitutes": false
    },
    "fromCache": false,
    "usageRemaining": 2,
    "aiProvider": "google",
    "aiModel": "gemini-2.5-flash-lite",
    "xpAwarded": 3,
    "conversationId": "string",
    "macrosValidated": true
  }
}
```

`xpAwarded` is the multi-occurrence Pulse AI usage XP. It is returned for both cache hits and fresh generations after a successful request.

`conversationId` is the `_id` of the `Conversation` document the message was saved to. `macrosValidated` indicates whether the recipe's declared macros passed mathematical validation against its ingredients — it is `false` when the discrepancy exceeds 15% even after the automatic retry. A `macrosValidated: false` response does not block the recipe from being returned; it is only an indicator of reduced accuracy.

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Pulse AI recipe generated or returned from cache successfully |
| `400` | Request body failed validation or the normalized message was empty |
| `401` | Missing, invalid, or expired Bearer token |
| `404` | Authenticated user record not found |
| `429` | Daily Pulse AI limit reached for a non-Pro user |
| `500` | AI provider returned invalid JSON after retry, or another unexpected server error occurred |
| `503` | Configured AI provider is temporarily unavailable |

### GET /pulse-ai/usage

Returns the current authenticated user's daily Pulse AI usage counters.

**JWT required:** Yes

**Request parameters**

None.

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "dailyAiUsage": 1,
    "isPro": false
  }
}
```

The controller resets the counter automatically when the user's local day changes before returning the current values.

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Pulse AI usage returned successfully |
| `401` | Missing, invalid, or expired Bearer token |
| `404` | Authenticated user record not found |
| `500` | Unexpected server error |

### DELETE /pulse-ai/cache

Invalidates all Pulse AI cache entries owned by the authenticated user.

**JWT required:** Yes

**Request parameters**

None.

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "deletedCount": 4
  }
}
```

**Status codes**

| Status | Meaning |
|---|---|
| `200` | User cache invalidated successfully |
| `401` | Missing, invalid, or expired Bearer token |
| `500` | Unexpected server error |

---

## Pantry Scanner

### POST /pantry-scanner/analyze

Analyzes a pantry image, filters low-confidence ingredients, and generates up to two Pulse AI recipes from the usable ingredients.

**JWT required:** Yes

**Request body**

| Field | Type | Required | Notes |
|---|---|---|---|
| `imageBase64` | string | Yes | Base64 image payload with an effective maximum size of approximately 2 MB |
| `mimeType` | string | Yes | One of `image/jpeg` or `image/png` |

**Processing pipeline**

1. Validates the request body with `pantryScanSchema` and calculates the image size in KB for structured logging.
2. Loads the authenticated user and checks `isPro`; Pro users skip the free-tier monthly limit gate.
3. For free-tier users, if `Date.now() > scanResetDate`, resets `scanCount` to `0` and recalculates `scanResetDate` with `addMonths(previousScanResetDate, 1)`, preserving the original billing cycle anchor.
4. If the current free-tier `scanCount >= 3`, returns `429 Too Many Requests` with code `scanner/monthly-limit-reached` plus `scansUsed`, `scansLimit`, and `resetDate`.
5. Calls `callVisionAi` with the pantry-analysis prompt built by `pantryPromptBuilder.service.ts`.
6. Parses and validates the returned JSON; if `noFoodDetected: true`, returns `200 OK` with empty arrays and does not increment the scanner.
7. Filters the detected ingredients to confidence levels `high` and `medium`; if the filtered array becomes empty, returns `200 OK` with `noUsableIngredients: true` and does not increment the scanner.
8. Only after valid ingredients are confirmed, increments `scanCount` atomically with `$inc`, then generates recipes through `promptBuilder.service.ts` and returns the Pantry Scanner XP payload.

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "ingredients": [
      {
        "name": "Banana",
        "estimatedQuantity": "2 units",
        "confidence": "high"
      },
      {
        "name": "Greek yogurt",
        "estimatedQuantity": "1 pot",
        "confidence": "medium"
      }
    ],
    "analysisNotes": "Detected fruit and dairy items suitable for smoothies.",
    "noFoodDetected": false,
    "recipes": [
      {
        "title": "Banana Yogurt Boost",
        "ingredients": [
          {
            "name": "Banana",
            "amount": "2 units"
          },
          {
            "name": "Greek yogurt",
            "amount": "1 pot"
          }
        ],
        "macros": {
          "protein": 24,
          "carbs": 36,
          "fat": 4,
          "calories": 260
        },
        "prepTimeSeconds": 60,
        "blendInstruction": "Blend for 45 seconds until smooth.",
        "tip": "Add ice for a thicker texture.",
        "hasSubstitutes": false
      }
    ],
    "scansUsed": 2,
    "scansLimit": 3,
    "resetDate": "2026-07-10T12:00:00.000Z",
    "xpAwarded": 5
  }
}
```

When `noFoodDetected` or `noUsableIngredients` is returned, the response still uses `200 OK` with empty `ingredients` and `recipes`, keeps `scansUsed` unchanged, and does not award XP.

`xpAwarded` is the multi-occurrence Pantry Scanner XP granted only after a successful scan with at least one usable ingredient.

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Pantry image analyzed successfully, or no food / no usable ingredients was confirmed |
| `400` | Request body failed validation |
| `401` | Missing, invalid, or expired Bearer token |
| `404` | Authenticated user record not found |
| `429` | Free-tier monthly Pantry Scanner limit reached |
| `500` | Vision provider returned invalid JSON after parsing |
| `503` | Vision provider is temporarily unavailable |

---

## Daily Missions

### GET /daily-missions

Returns the authenticated user's daily missions for the current local day, generating the document on demand when it does not exist yet.

**JWT required:** Yes

**Request parameters**

None.

Mission generation uses a goal-based weighted pool with dynamic filtering so impossible missions are removed before selection, while fallback mission types ensure that exactly three unique missions are always returned.

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "dailyMission": {
      "missionDate": "2026-06-26",
      "missions": [
        {
          "missionId": "string",
          "type": "makeBlend",
          "titleKey": "missions.makeBlend.title",
          "descriptionKey": "missions.makeBlend.description",
          "requirement": 1,
          "progress": 0,
          "completed": false,
          "xpReward": 15
        },
        {
          "missionId": "string",
          "type": "usePulseAI",
          "titleKey": "missions.usePulseAI.title",
          "descriptionKey": "missions.usePulseAI.description",
          "requirement": 1,
          "progress": 0,
          "completed": false,
          "xpReward": 10
        },
        {
          "missionId": "string",
          "type": "scanPantry",
          "titleKey": "missions.scanPantry.title",
          "descriptionKey": "missions.scanPantry.description",
          "requirement": 1,
          "progress": 0,
          "completed": false,
          "xpReward": 15
        }
      ],
      "bonusAwarded": false,
      "xpAvailable": 60,
      "completedCount": 0
    }
  }
}
```

`xpAvailable` is calculated as the sum of all incomplete mission `xpReward` values plus `20` more when `bonusAwarded` is still `false`.

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Daily missions returned successfully |
| `401` | Missing, invalid, or expired Bearer token |
| `404` | Authenticated user record not found |
| `500` | Unexpected server error |

---

## Shopping Lists

### GET /shopping-lists

Returns the authenticated user's active shopping lists as `ShoppingListSummary` items.

**JWT required:** Yes

**Request parameters**

None.

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "lists": [
      {
        "id": "string",
        "name": "Weekly groceries",
        "isArchived": false,
        "createdAt": "2026-06-20T10:00:00.000Z",
        "updatedAt": "2026-06-26T09:15:00.000Z",
        "totalItems": 6,
        "pendingItems": 4
      }
    ],
    "canCreateMore": false
  }
}
```

`canCreateMore` is `true` when the user is Pro or when a free-tier user has fewer than one active list.

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Active shopping lists returned successfully |
| `401` | Missing, invalid, or expired Bearer token |
| `404` | Authenticated user record not found |
| `500` | Unexpected server error |

### GET /shopping-lists/archived

Returns the authenticated user's archived shopping lists.

**JWT required:** Yes

**Request parameters**

None.

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "lists": [
      {
        "id": "string",
        "name": "Trip supplies",
        "isArchived": true,
        "createdAt": "2026-06-10T10:00:00.000Z",
        "updatedAt": "2026-06-18T12:30:00.000Z",
        "totalItems": 3,
        "pendingItems": 0
      }
    ]
  }
}
```

All returned lists have `isArchived: true`.

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Archived shopping lists returned successfully |
| `401` | Missing, invalid, or expired Bearer token |
| `404` | Authenticated user record not found |
| `500` | Unexpected server error |

### GET /shopping-lists/:listId

Returns a full `ShoppingListDetail` payload for a single list.

**JWT required:** Yes

**Path parameters**

| Field | Type | Required | Notes |
|---|---|---|---|
| `listId` | string | Yes | MongoDB ObjectId of the shopping list |

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "shoppingList": {
      "id": "string",
      "name": "Weekly groceries",
      "isArchived": false,
      "createdAt": "2026-06-20T10:00:00.000Z",
      "updatedAt": "2026-06-26T09:15:00.000Z",
      "totalItems": 2,
      "pendingItems": 1,
      "items": [
        {
          "itemId": "string",
          "name": "Bananas",
          "quantity": "6",
          "checked": false,
          "addedAt": "2026-06-26T09:10:00.000Z",
          "source": "manual"
        }
      ]
    }
  }
}
```

Items are returned sorted by `addedAt` descending.

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Shopping list returned successfully |
| `400` | Shopping list id is not a valid MongoDB ObjectId |
| `401` | Missing, invalid, or expired Bearer token |
| `403` | Shopping list exists but does not belong to the authenticated user |
| `404` | Shopping list not found |
| `500` | Unexpected server error |

### POST /shopping-lists

Creates a new active shopping list.

**JWT required:** Yes

**Request body**

| Field | Type | Required | Notes |
|---|---|---|---|
| `name` | string | Yes | 1 to 80 characters |

Before creating the document, the backend checks the free-tier active-list limit. Free-tier users can keep only one active list at a time.

**Success response**

**Status:** `201 Created`

```json
{
  "success": true,
  "data": {
    "shoppingList": {
      "id": "string",
      "name": "Weekly groceries",
      "isArchived": false,
      "createdAt": "2026-06-26T09:15:00.000Z",
      "updatedAt": "2026-06-26T09:15:00.000Z",
      "totalItems": 0,
      "pendingItems": 0,
      "items": []
    }
  }
}
```

If a free-tier user already has one active list, the endpoint returns `403 Forbidden` with code `shoppingList/free-tier-limit` and `upgradeRequired: true`.

**Status codes**

| Status | Meaning |
|---|---|
| `201` | Shopping list created successfully |
| `400` | Request body failed validation |
| `401` | Missing, invalid, or expired Bearer token |
| `403` | Free-tier active-list limit reached |
| `404` | Authenticated user record not found |
| `500` | Unexpected server error |

### PATCH /shopping-lists/:listId

Partially updates a shopping list.

**JWT required:** Yes

**Path parameters**

| Field | Type | Required | Notes |
|---|---|---|---|
| `listId` | string | Yes | MongoDB ObjectId of the shopping list |

**Request body**

All fields are optional.

| Field | Type | Required | Notes |
|---|---|---|---|
| `name` | string | No | 1 to 80 characters |
| `isArchived` | boolean | No | Set to `true` to archive or `false` to restore |

When restoring an archived list with `isArchived: false`, the backend re-checks the free-tier active-list limit before allowing the change.

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "shoppingList": {
      "id": "string",
      "name": "Weekly groceries",
      "isArchived": true,
      "createdAt": "2026-06-20T10:00:00.000Z",
      "updatedAt": "2026-06-26T09:40:00.000Z",
      "totalItems": 6,
      "pendingItems": 2,
      "items": []
    }
  }
}
```

If no supported fields are sent, the current shopping list is returned unchanged.

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Shopping list updated successfully |
| `400` | Request body failed validation or shopping list id is invalid |
| `401` | Missing, invalid, or expired Bearer token |
| `403` | Shopping list does not belong to the authenticated user, or free-tier restore limit was reached |
| `404` | Shopping list or authenticated user not found |
| `500` | Unexpected server error |

### DELETE /shopping-lists/:listId

Deletes a shopping list permanently.

**JWT required:** Yes

**Path parameters**

| Field | Type | Required | Notes |
|---|---|---|---|
| `listId` | string | Yes | MongoDB ObjectId of the shopping list |

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "message": "Shopping list deleted successfully."
  }
}
```

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Shopping list deleted successfully |
| `400` | Shopping list id is not a valid MongoDB ObjectId |
| `401` | Missing, invalid, or expired Bearer token |
| `403` | Shopping list exists but does not belong to the authenticated user |
| `404` | Shopping list not found |
| `500` | Unexpected server error |

### PUT /shopping-lists/:listId/items

Replaces the entire items array of a shopping list.

**JWT required:** Yes

**Path parameters**

| Field | Type | Required | Notes |
|---|---|---|---|
| `listId` | string | Yes | MongoDB ObjectId of the shopping list |

**Request body**

Root payload: array of shopping list items, maximum 100 entries.

| Field | Type | Required | Notes |
|---|---|---|---|
| `itemId` | string | No | Existing id to preserve, or temporary id such as `temp_...` |
| `name` | string | Yes | 1 to 100 characters |
| `quantity` | string | No | Maximum 50 characters |
| `checked` | boolean | No | Defaults to `false` |
| `source` | string | No | `manual` or `recipe`; defaults to `manual` |

For items with an existing non-temporary `itemId`, the backend preserves both `itemId` and `addedAt`. For items without `itemId` or with an id starting with `temp_`, the backend generates a fresh `crypto.randomUUID()` and a new `addedAt` timestamp.

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "shoppingList": {
      "id": "string",
      "name": "Weekly groceries",
      "isArchived": false,
      "createdAt": "2026-06-20T10:00:00.000Z",
      "updatedAt": "2026-06-26T09:50:00.000Z",
      "totalItems": 2,
      "pendingItems": 1,
      "items": [
        {
          "itemId": "string",
          "name": "Bananas",
          "quantity": "6",
          "checked": false,
          "addedAt": "2026-06-26T09:10:00.000Z",
          "source": "manual"
        }
      ]
    }
  }
}
```

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Shopping list items replaced successfully |
| `400` | Request body failed validation or shopping list id is invalid |
| `401` | Missing, invalid, or expired Bearer token |
| `403` | Shopping list exists but does not belong to the authenticated user |
| `404` | Shopping list not found |
| `500` | Unexpected server error |

### PATCH /shopping-lists/:listId/items/:itemId/check

Atomically toggles the `checked` state of a single shopping list item.

**JWT required:** Yes

**Path parameters**

| Field | Type | Required | Notes |
|---|---|---|---|
| `listId` | string | Yes | MongoDB ObjectId of the shopping list |
| `itemId` | string | Yes | Existing shopping list item id |

The controller resolves the current item state first, then applies the inverse value with MongoDB's positional update operator.

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "item": {
      "itemId": "string",
      "name": "Bananas",
      "quantity": "6",
      "checked": true,
      "addedAt": "2026-06-26T09:10:00.000Z",
      "source": "manual"
    }
  }
}
```

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Shopping list item toggled successfully |
| `400` | Shopping list id is not a valid MongoDB ObjectId |
| `401` | Missing, invalid, or expired Bearer token |
| `403` | Shopping list exists but does not belong to the authenticated user |
| `404` | Shopping list or shopping list item not found |
| `500` | Unexpected server error |

### DELETE /shopping-lists/:listId/items/checked

Removes all checked items from a shopping list.

**JWT required:** Yes

**Path parameters**

| Field | Type | Required | Notes |
|---|---|---|---|
| `listId` | string | Yes | MongoDB ObjectId of the shopping list |

The controller computes `removedCount` before applying an atomic `$pull` that removes every item with `checked: true`.

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "removedCount": 2,
    "shoppingList": {
      "id": "string",
      "name": "Weekly groceries",
      "isArchived": false,
      "createdAt": "2026-06-20T10:00:00.000Z",
      "updatedAt": "2026-06-26T10:05:00.000Z",
      "totalItems": 1,
      "pendingItems": 1,
      "items": [
        {
          "itemId": "string",
          "name": "Peanut butter",
          "checked": false,
          "addedAt": "2026-06-26T10:00:00.000Z",
          "source": "recipe"
        }
      ]
    }
  }
}
```

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Checked shopping list items removed successfully |
| `400` | Shopping list id is not a valid MongoDB ObjectId |
| `401` | Missing, invalid, or expired Bearer token |
| `403` | Shopping list exists but does not belong to the authenticated user |
| `404` | Shopping list not found |
| `500` | Unexpected server error |

---

## Purchases and Subscriptions

### POST /purchases/verify

Validates a purchase or subscription state with RevenueCat and syncs the authenticated user's Pro status.

**JWT required:** Yes

**Request body**

| Field | Type | Required | Notes |
|---|---|---|---|
| `platform` | string | No | One of `ios`, `android`; required when `receipt` is present |
| `receipt` | string | No | Purchase receipt token generated by the RevenueCat mobile SDK after a successful purchase |
| `productId` | string | No | Required when `platform` is `android` and `receipt` is present |

When `receipt` is present, the backend posts it to `POST https://api.revenuecat.com/v1/receipts` using `REVENUECAT_API_KEY` to validate the purchase and extract the active entitlement. When `receipt` is omitted, the backend instead calls `GET https://api.revenuecat.com/v1/subscribers/{userId}` to resynchronize the user's current subscription state from RevenueCat without a new purchase — used by the mobile app to restore or refresh Pro status. In both cases, if an active Pulse Pro entitlement is found, the backend updates the user with `isPro: true`, `subscriptionPlan` (`monthly` or `annual`), `subscriptionExpiresAt`, `subscriptionId`, and `revenueCatCustomerId`, and returns the updated profile.

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "user": {
      "id": "string",
      "email": "string",
      "name": "string",
      "profilePhoto": "https://cdn.example.com/avatar.png",
      "blendiModel": "Lite | ProPlus | Steel",
      "goal": "Muscle | Wellness | Energy | Recovery",
      "locale": "en | pt-BR",
      "unitSystem": "metric | imperial",
      "timezone": "America/Sao_Paulo",
      "dailyProteinTarget": 160,
      "dailyCalorieTarget": 2500,
      "dailyCarbTarget": 240,
      "dailyHydrationTarget": 3000,
      "isPro": true,
      "subscriptionId": "string",
      "subscriptionPlan": "monthly | annual",
      "subscriptionExpiresAt": "2026-07-26T14:20:00.000Z",
      "revenueCatCustomerId": "string",
      "subscriptionCancelRequestedAt": null,
      "longestStreak": 12,
      "weight": 78,
      "height": 180,
      "createdAt": "2026-04-29T12:34:56.000Z",
      "updatedAt": "2026-06-26T14:20:00.000Z"
    }
  }
}
```

If `REVENUECAT_API_KEY` is not configured, the endpoint returns `503` with code `purchases/system-unavailable`. If RevenueCat resolves no active Pulse Pro entitlement for the receipt, it returns `400` with code `purchases/invalid-receipt`. If the RevenueCat API itself returns an error, the endpoint returns `400` for a 4xx RevenueCat response or `502` for a 5xx RevenueCat response, both with code `purchases/provider-error`.

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Purchase or subscription state verified and user profile updated successfully |
| `400` | Request body failed Zod validation, RevenueCat found no active Pulse Pro entitlement, or RevenueCat returned a 4xx error |
| `401` | Missing, invalid, or expired Bearer token |
| `404` | Authenticated user record not found |
| `500` | Unexpected server error |
| `502` | RevenueCat returned a 5xx error |
| `503` | `REVENUECAT_API_KEY` is not configured |

### POST /webhooks/revenuecat

Receives signed subscription lifecycle events from RevenueCat and synchronizes the corresponding user's subscription state. This endpoint is public and called directly by RevenueCat, not by the mobile client. It is mounted before the global JSON body parser so the raw request body can be used to verify the signature.

**JWT required:** No

**Request headers**

| Field | Type | Required | Notes |
|---|---|---|---|
| `X-RevenueCat-Webhook-Signature` | string | Yes | HMAC-SHA256 signature in the `t=<timestamp>,v1=<signature>` format, validated against `REVENUECAT_WEBHOOK_SECRET` with a 5-minute timestamp tolerance |

**Request body**

The payload follows RevenueCat's webhook event schema. Relevant fields:

| Field | Type | Required | Notes |
|---|---|---|---|
| `event.id` | string | Yes | RevenueCat event identifier |
| `event.type` | string | Yes | Only `INITIAL_PURCHASE`, `RENEWAL`, `CANCELLATION`, and `EXPIRATION` are processed; other types are acknowledged and ignored |
| `event.app_user_id` | string | Yes | RevenueCat app user id, matched against the local user by `_id` or `revenueCatCustomerId` |
| `event.app_id` | string | No | Compared against `REVENUECAT_APP_ID` when configured; mismatched events are ignored |
| `event.expiration_at_ms` | number | No | Used to set `subscriptionExpiresAt` on `CANCELLATION` and `EXPIRATION` |
| `event.original_transaction_id` | string | No | Used to set `subscriptionId` on `CANCELLATION` |

**Processing pipeline**

1. Returns `503` if `REVENUECAT_API_KEY` or `REVENUECAT_WEBHOOK_SECRET` is not configured.
2. Returns `401` if the signature header is missing or does not match the computed HMAC signature.
3. Parses and validates the JSON payload against the webhook schema; returns `400` on mismatch.
4. Returns `200` with `ignored: true` when `app_id` does not match `REVENUECAT_APP_ID`, when the event type is not one of the four handled types, or when no local user matches `app_user_id`, `original_app_user_id`, or `aliases`.
5. For `INITIAL_PURCHASE` and `RENEWAL`, resynchronizes the user's active subscription from RevenueCat and sets `isPro: true`.
6. For `CANCELLATION`, attempts the same resynchronization first; if RevenueCat reports no active entitlement, records `subscriptionCancelRequestedAt` instead — `isPro` is not changed, so access is kept active until expiration.
7. For `EXPIRATION`, attempts the same resynchronization first in case a new subscription is already active; if none is found and the event's expiration timestamp has passed, sets `isPro: false` and clears `subscriptionCancelRequestedAt`.

The endpoint returns `200` for every processed or ignored case so RevenueCat does not retry delivery.

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "processed": true,
    "eventId": "string",
    "eventType": "INITIAL_PURCHASE | RENEWAL | CANCELLATION | EXPIRATION"
  }
}
```

Ignored events also return `200 OK`, with a different payload:

```json
{
  "success": true,
  "data": {
    "ignored": true,
    "reason": "string"
  }
}
```

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Event processed or intentionally ignored |
| `400` | Webhook payload failed schema validation |
| `401` | Missing or invalid `X-RevenueCat-Webhook-Signature` header |
| `503` | `REVENUECAT_API_KEY` or `REVENUECAT_WEBHOOK_SECRET` is not configured |

---

## Profile Photo

### POST /users/profile-photo

Uploads or replaces the authenticated user's profile photo.

**JWT required:** Yes

**Request body**

| Field | Type | Required | Notes |
|---|---|---|---|
| `imageBase64` | string | Yes | Base64-encoded image payload; maximum 530,000 characters, equivalent to roughly 400 KB of image data after base64 encoding |
| `fileType` | string | Yes | One of `jpeg`, `png`; case-insensitive, normalized to lowercase |

The backend performs an atomic upsert on the dedicated `user_photos` collection, storing `imageBase64` and `fileType`, and updates `hasProfilePhoto: true` and `profilePhotoUpdatedAt` on the `User` document in the same MongoDB transaction.

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "user": {
      "id": "string",
      "email": "string",
      "name": "string",
      "profilePhoto": null,
      "hasProfilePhoto": true,
      "profilePhotoUpdatedAt": "2026-06-26T14:20:00.000Z",
      "updatedAt": "2026-06-26T14:20:00.000Z"
    }
  }
}
```

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Profile photo stored successfully |
| `400` | Request body failed Zod validation |
| `401` | Missing, invalid, or expired Bearer token |
| `404` | Authenticated user record not found |
| `413` | `imageBase64` exceeds the maximum allowed length; code `profilePhoto/file-too-large` |
| `500` | Unexpected server error |

### GET /users/me/photo

Returns the authenticated user's stored profile photo payload.

**JWT required:** Yes

**Request parameters**

None.

The photo is fetched from the dedicated `user_photos` collection and returns only `imageBase64` and `mimeType`. It is never included in the `GET /users/me` response, keeping profile responses lightweight.

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "imageBase64": "string",
    "mimeType": "image/jpeg | image/png"
  }
}
```

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Profile photo returned successfully |
| `401` | Missing, invalid, or expired Bearer token |
| `404` | Authenticated user record not found, or no profile photo exists; code `profilePhoto/not-found` for the missing-photo case |
| `500` | Unexpected server error |

### DELETE /users/profile-photo

Deletes the authenticated user's stored profile photo.

**JWT required:** Yes

**Request parameters**

None.

Deletes the document from `user_photos` and updates `hasProfilePhoto: false` and `profilePhotoUpdatedAt` on the `User` document in the same transaction.

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "user": {
      "id": "string",
      "email": "string",
      "name": "string",
      "profilePhoto": null,
      "hasProfilePhoto": false,
      "profilePhotoUpdatedAt": "2026-06-26T14:25:00.000Z",
      "updatedAt": "2026-06-26T14:25:00.000Z"
    }
  }
}
```

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Profile photo removed successfully |
| `401` | Missing, invalid, or expired Bearer token |
| `404` | Authenticated user record not found |
| `500` | Unexpected server error |

---

## Conversation History

### GET /conversations

Returns metadata for the authenticated user's most recent conversations, ordered from newest to oldest.

**JWT required:** Yes

**Request parameters**

None.

Returns up to the last 20 conversations. Each item is a summary only — the full `messages` array is never included in this endpoint, keeping the history list lightweight.

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "conversations": [
      {
        "id": "string",
        "createdAt": "2026-06-26T14:20:00.000Z",
        "lastRecipeName": "Berry Blast",
        "messageCount": 4,
        "daysAgo": 0
      }
    ]
  }
}
```

`daysAgo` is computed on the backend in the authenticated user's timezone: `0` for today, `1` for yesterday, and so on.

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Conversation history returned successfully |
| `401` | Missing, invalid, or expired Bearer token |
| `404` | Authenticated user record not found |
| `500` | Unexpected server error |

### GET /conversations/:id

Returns a single conversation with its full message history.

**JWT required:** Yes

**Path parameters**

| Field | Type | Required | Notes |
|---|---|---|---|
| `id` | string | Yes | MongoDB ObjectId of the conversation |

The backend verifies that the conversation's `userId` matches the authenticated user before returning it.

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "conversation": {
      "id": "string",
      "createdAt": "2026-06-26T14:20:00.000Z",
      "updatedAt": "2026-06-26T14:22:00.000Z",
      "lastRecipeName": "Berry Blast",
      "messages": [
        {
          "role": "user",
          "content": "Give me a high-protein recipe",
          "timestamp": "2026-06-26T14:20:00.000Z"
        },
        {
          "role": "assistant",
          "content": {
            "title": "Berry Blast",
            "ingredients": [
              { "name": "Blueberries", "amount": "100 g" }
            ],
            "macros": { "protein": 30, "carbs": 22, "fat": 6, "calories": 280 },
            "prepTimeSeconds": 60,
            "blendInstruction": "Blend for 45 seconds.",
            "tip": "Use frozen fruit for a thicker texture.",
            "hasSubstitutes": false
          },
          "timestamp": "2026-06-26T14:20:05.000Z"
        }
      ]
    }
  }
}
```

`content` is a plain string for `user` messages and a full `PulseAiRecipe` object for `assistant` messages.

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Conversation returned successfully |
| `400` | Conversation id is not a valid MongoDB ObjectId |
| `401` | Missing, invalid, or expired Bearer token |
| `403` | Conversation exists but does not belong to the authenticated user |
| `404` | Conversation not found |
| `500` | Unexpected server error |

---

## Weekly Reports

### GET /weekly-reports/latest

Returns the authenticated user's most recent weekly report.

**JWT required:** Yes

**Request parameters**

None.

If no report exists yet, returns `hasReport: false` with `nextReportDate` set to the next Monday at 9am in the user's timezone. If a report exists, returns `hasReport: true` with the full report document. The `data` object is returned in full for every user regardless of plan — restricting the preview for free-tier users is the mobile app's responsibility, not the backend's.

**Success response — no report yet**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "hasReport": false,
    "nextReportDate": "2026-06-29T12:00:00.000Z"
  }
}
```

**Success response — report available**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "hasReport": true,
    "id": "string",
    "weekStartDate": "2026-06-15",
    "weekEndDate": "2026-06-21",
    "isProAtGeneration": true,
    "data": {
      "nutrition": {
        "blendCount": 10,
        "avgProteinPerDay": 62,
        "proteinGoalHitDays": 5,
        "calorieGoalHitDays": 4,
        "bestDay": { "date": "2026-06-18", "proteinAmount": 90 },
        "highlightRecipe": {
          "name": "Berry Blast",
          "protein": 30,
          "carbs": 22,
          "fat": 6,
          "calories": 280,
          "rating": 5
        }
      },
      "hydration": {
        "totalMl": 15000,
        "avgDailyMl": 2143,
        "goalHitDays": 5,
        "dailyBreakdown": [2500, 2200, 1800, 2500, 2000, 2300, 1700]
      },
      "supplements": {
        "adherenceRate": 0.86,
        "perfectDays": 5,
        "bySupplementName": { "Creatine": 7, "Omega-3": 5 },
        "topSupplement": "Creatine",
        "bottomSupplement": "Omega-3"
      },
      "gamification": {
        "xpEarned": 320,
        "currentLevel": 6,
        "missionsCompleted": 12,
        "blendDaysInWeek": 6,
        "currentStreak": 9,
        "streakBrokenOnDate": null,
        "levelUpOccurred": false
      }
    },
    "previousWeekComparison": {
      "avgProteinPerDayDeltaPercent": 8.5,
      "avgDailyMlDeltaPercent": -3.2,
      "adherenceRateDeltaPercent": 5.0
    },
    "createdAt": "2026-06-22T09:00:00.000Z"
  }
}
```

`data` has four sub-objects: `nutrition`, `hydration`, `supplements`, and `gamification`. `previousWeekComparison` is present only when a report for the immediately preceding week also exists. `isProAtGeneration` snapshots the user's plan at generation time and does not change retroactively if the user later upgrades or downgrades.

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Weekly report status returned successfully, with or without an existing report |
| `401` | Missing, invalid, or expired Bearer token |
| `404` | Authenticated user record not found |
| `500` | Unexpected server error |

### GET /weekly-reports

Returns the report for one specific week.

**JWT required:** Yes

**Query parameters**

| Field | Type | Required | Notes |
|---|---|---|---|
| `weekStart` | string | Yes | Date in `YYYY-MM-DD` format identifying the report's `weekStartDate` |

**Success response**

**Status:** `200 OK`

Same `data` shape as the "report available" response of `GET /weekly-reports/latest`, without the `hasReport` field.

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Weekly report returned successfully |
| `400` | `weekStart` is missing or not in `YYYY-MM-DD` format |
| `401` | Missing, invalid, or expired Bearer token |
| `404` | No report exists for the requested week; code `weeklyReport/not-found` |
| `500` | Unexpected server error |

### GET /weekly-reports/dates

Returns the list of weeks for which the authenticated user has a generated report.

**JWT required:** Yes

**Request parameters**

None.

Used by the mobile week selector to know which weeks have a report available.

**Success response**

**Status:** `200 OK`

```json
{
  "success": true,
  "data": {
    "dates": ["2026-06-01", "2026-06-08", "2026-06-15"]
  }
}
```

Dates are `weekStartDate` values in `YYYY-MM-DD` format, sorted chronologically.

**Status codes**

| Status | Meaning |
|---|---|
| `200` | Report dates returned successfully |
| `401` | Missing, invalid, or expired Bearer token |
| `500` | Unexpected server error |

---

## Health

### GET /ping

Public health check endpoint used to confirm server availability and verify that a new Railway deploy is actually live.

**JWT required:** No

**Request parameters**

None.

**Success response**

**Status:** `200 OK`

```json
{
  "status": "ok",
  "version": "0.1.0",
  "environment": "development",
  "timestamp": "2026-04-29T12:34:56.000Z"
}
```

**Response fields**

| Field | Type | Description |
|---|---|---|
| `status` | string | Health indicator; current value is `ok` |
| `version` | string | API version from environment configuration; essential to confirm that a new Railway deploy was applied successfully |
| `environment` | string | Runtime environment such as `development`, `staging`, or `production` |
| `timestamp` | string | Current server time in ISO 8601 UTC format |

**Status codes**

| Status | Meaning |
|---|---|
| `200` | API is reachable and responding |

---

## Error Response Format

### Standard Error Envelope

Most error responses follow this format:

```json
{
  "success": false,
  "code": "auth/invalid-credentials",
  "message": "Invalid email or password."
}
```

### Validation Errors

When a request fails Zod validation, the API returns `400 Bad Request` and includes an `errors` array describing each invalid field.

```json
{
  "success": false,
  "code": "validation/invalid-input",
  "message": "Validation failed.",
  "errors": [
    {
      "field": "email",
      "message": "errors.validation.email_invalid"
    },
    {
      "field": "password",
      "message": "errors.validation.too_short",
      "minimum": 8
    }
  ]
}
```

**Validation error fields**

| Field | Type | Description |
|---|---|---|
| `field` | string | Request field that failed validation |
| `message` | string | i18n key describing the validation failure |
| `minimum` | number | Present when the failure comes from a minimum constraint |
| `maximum` | number | Present when the failure comes from a maximum constraint |

### Development Diagnostics

Unhandled server errors pass through the global error middleware and may include a `stack` field when `NODE_ENV=development`.

```json
{
  "success": false,
  "code": "network/internal-server-error",
  "message": "Internal server error.",
  "stack": "Error: ..."
}
```

---

## Planned Endpoints

The following endpoint groups are planned for later phases and are not implemented yet.

| Phase | Planned group |
|---|---|
| Phase 4 | Golden Ticket QR endpoints |
| Phase 3 | Biometric Sync endpoints |
| Phase 3 | Seamless Refill endpoints |